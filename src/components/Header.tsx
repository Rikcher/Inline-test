import React from 'react';

const Header: React.FC = () => {
    
    return (
        <header className=''>
            <nav className=''>
                <p>header</p>
            </nav>
        </header>
    );
};

export default Header;
